const assert=require("assert");const {JsonUserRepository}=require("../repositories/json-user-repository");const {AuthService,hashPassword}=require("../services/auth-service");
const passwordHash=hashPassword("unchanged");const store={users:[{userId:"admin-id",username:"admin",role:"OWNER",status:"DISABLED",passwordHash,failedLoginCount:5,lockedUntil:"2030-01-01T00:00:00.000Z"},{userId:"other",username:"other",role:"STAFF",status:"DISABLED",passwordHash:hashPassword("otherpass")}],bills:[{id:"b"}],tables:[{id:1}],members:[{id:"m"}]};const before=JSON.stringify({business:{bills:store.bills,tables:store.tables,members:store.members},other:store.users[1]});const repo=new JsonUserRepository({getStore:()=>store,save:()=>{}});const auth=new AuthService(repo,()=>new Date("2026-01-01T00:00:00.000Z"));assert.deepStrictEqual(auth.emergencyReactivateOwner(),{status:"reactivated"});const admin=repo.findByUsername("admin");assert.strictEqual(admin.status,"ACTIVE");assert.strictEqual(admin.role,"OWNER");assert.strictEqual(admin.username,"admin");assert.strictEqual(admin.passwordHash,passwordHash);assert.strictEqual(admin.failedLoginCount,0);assert.strictEqual(admin.lockedUntil,null);assert.strictEqual(JSON.stringify({business:{bills:store.bills,tables:store.tables,members:store.members},other:store.users[1]}),before);assert.strictEqual(store.users.length,2);console.log("Emergency OWNER reactivation tests passed");
