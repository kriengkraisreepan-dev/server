const assert=require("assert"); const {normalizeSettings}=require("../services/settings-service"); const base=normalizeSettings({hourlyRate:100,minimumCharge:1,tableCount:1}); assert.strictEqual(base.security.timeoutMinutes,480); assert.strictEqual(base.security.warningMinutes,5); assert.ok(base.security.timeoutMinutes>base.security.warningMinutes); console.log("Sprint 7 session settings compatibility tests passed");
