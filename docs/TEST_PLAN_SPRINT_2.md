# Sprint 2 — Test Plan

Automated foundation coverage in `tests/sprint2-runtime-billing.test.js` covers runtime table mapping, open/pause/resume/close/release, invalid cancellation after close, integer-satang pricing, hourly regression cases, and the expected final-rounding difference.

Manual/next-sprint coverage: live API mutation against a copy of user data, receipt visual output on a printer, legacy in-progress table conversion after restart, QR-awaiting-payment path, ESP32 relay hardware, and multi-process JSON write contention.
